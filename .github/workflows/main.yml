on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 'testing GitHub action'
        run: pwd

      - name: 'testing print'
        run: echo mohamed
