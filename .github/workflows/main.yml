on:
  push:
    branches:
      - master






  jobs:
    build:
      runs-on: ubuntu-latest

      steps:
        - name: 'testing gethub action'
         run: pwd

        - name: 'testing print'
         run: eco mohamed
